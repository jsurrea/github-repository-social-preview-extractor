{
  "name": "github-repository-social-preview-extractor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Extract and download GitHub's auto-generated social preview images from any public repository",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "^6.1.0"
  }
}
